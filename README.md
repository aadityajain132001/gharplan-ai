# GharPlan AI
No-login, no-payment home design MVP.

## Render
Runtime: Python 3.13 (`runtime.txt`)
Build command: `pip install -r requirements.txt`
Start command: `fastapi run main.py --host 0.0.0.0 --port $PORT`
